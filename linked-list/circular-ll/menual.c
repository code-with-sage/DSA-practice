#include "cll.h"

void printmenu(){
    printf("===================  MANUAL ======================\n\n");
    printf("q                 || quit the program \n");
    printf("insl              || create and add nodes in last of the list\n");
    printf("emp               || Checks whether the linked list is empty.\n");
    printf("prnt              || Displays all nodes in the linked list.\n");
    printf("insertlast        || Inserts a node at the end of the linked list.\n");
    printf("deleteEle         || Deletes the node with the given roll number.\n");
    printf("searchByRoll      || Searches and returns the node with the given roll number.\n");
    printf("searchByName      || Searches and returns the node with the given name.\n\n");
    printf("==================================================\n");
}
