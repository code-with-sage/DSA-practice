#include "sort.h"

void quich_sort(int Arr[] , int lo , int hi){

    if (Arr == NULL){
        printf("Array is rpty shorting failed");
    }
    int lo;
    int hi
    int pid;

    while (lo < hi)
}